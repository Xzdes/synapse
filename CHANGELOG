# Журнал изменений Synapse

Этот документ отслеживает основные изменения и вехи в развитии проекта Synapse.

## [0.1.0] - 2025-08-25 - Первый рабочий прототип

Эта версия знаменует собой важнейший этап: создание первого рабочего прототипа, который доказывает жизнеспособность ключевых концепций Synapse. Проект был перестроен с нуля путем слияния идей из архитектурной спецификации Synapse и рабочего движка дифференцируемых вычислений из проекта-прототипа Rusty Gradients.

### ✨ Новые возможности и достижения

*   **Фундамент ASG**: Реализованы базовые структуры для представления кода в виде Абстрактного Синтаксического Графа (`ASG`, `Node`, `Edge`).
*   **Рабочий интерпретатор**: Создан простой интерпретатор, способный выполнять ASG.
*   **Два типа вычислений**: Интерпретатор поддерживает как базовую целочисленную арифметику (`Value::Int`), так и **дифференцируемые вычисления с тензорами**.
*   **Интеграция Autograd**: **Движок автоматического дифференцирования** из Rusty Gradients успешно интегрирован в среду выполнения Synapse как `Value::Tensor`.
*   **Первая дифференцируемая операция**: Реализована операция `TensorAdd`, которая корректно строит граф вычислений.
*   **Рабочий `backward()`**: Демонстрационный пример (`autograd_demo`) успешно выполняет прямой проход (forward pass), а затем и обратное распространение ошибки (backward pass), корректно вычисляя градиенты.

### 🎯 Фокус версии

Главная цель этого релиза — доказать, что архитектура Synapse, где дифференцируемый тензор является лишь одним из типов данных, работает на практике. Мы успешно создали "вертикальный срез", демонстрирующий полный цикл от построения ASG до его выполнения и вычисления градиентов.

---

## 🚧 Дорожная карта и будущие релизы

Теперь, когда фундамент заложен, дальнейшая работа будет сосредоточена на расширении возможностей языка и инструментария в следующих ключевых областях.

### ➡️ Следующий шаг: Версия 0.2.0 (Расширение ядра)

*   **Расширение операций с тензорами**: Реализовать полный набор математических операций из Rusty Gradients (`TensorSub`, `TensorMul`, `DotProduct`, `ReLU` и т.д.) в интерпретаторе.
*   **Фабрики узлов (`node_factories`)**: Реализовать удобные функции для программного создания ASG, чтобы упростить написание примеров и тестов.
*   **Сериализация `SYN1`**: Оживить бинарный формат. Написать тесты, которые сохраняют ASG в файл `.syn1`, загружают его обратно и успешно выполняют.
*   **Базовая система типов**: Начать реализацию `type_checker`, который сможет валидировать простые типы (`Int`, `Tensor`) в ASG перед выполнением.

### 🚀 Долгосрочное видение (Версии 0.3.0+)

*   **Система эффектов**: Научить интерпретатор обрабатывать узлы эффектов (`EffectConsole`, `EffectFSRead` и т.д.) через контролируемое Runtime API.
*   **Полноценная система типов**: Реализовать сложную систему типов из спецификации, включая полиморфизм, трейты и линейные типы.
*   **Система доказательств и верификации**: Интегрировать SMT-решатель (Z3) для выполнения узлов `Assert`, `Assume`, `Specification`.
*   **Бэкенды компилятора**: Начать реализацию кодогенерации (приоритет — LLVM или WebAssembly), превратив интерпретатор в полноценный компилятор.
*   **Конкурентность и асинхронность**: Реализовать модель безопасного параллелизма, описанную в спецификации.
*   **Метапрограммирование**: Реализовать гигиеническую макросистему, оперирующую напрямую с ASG.