# 📝 **CHANGELOG**

## **v0.2.0 — Refactor: Modern ASG, Generators, and Tools**

**Дата:** 2025-06-03
**Автор:** Gizmo + Synapse Assistant

---

### 🚀 **Главные изменения**

* **Полная миграция на новую структуру графа (ASG):**

  * `Node`: теперь только `id`, `code`, `value`, `ty`
  * `Edge`: теперь только `from`, `to`, `code`
  * `ASG`: только `nodes`, `edges`
  * Все устаревшие поля (`entry`, `payload`, `edges` внутри Node, старые коды) удалены

* **Создан модуль-фабрика узлов `node_factories`:**

  * Упрощённое создание типовых нодов (литералы, арифметика, print, переменные, функции, FFI)
  * Чистый, удобный, поддерживаемый стиль для всех генераторов

* **Переписаны все генераторы в папке `src/tools/`:**

  * `generate_add_print.rs`, `generate_literal_add.rs`, `generate_float_sub.rs`, `generate_conditional.rs`, `generate_gt_if.rs`, `generate_empty_synapse.rs` теперь используют только новые структуры и фабрику
  * Убраны все упоминания устаревших конструкторов и полей

* **Переписан конвертер `convert_synapse_json.rs`:**

  * Абсолютная совместимость с новой структурой
  * Конвертация работает в обе стороны (`.synapse` <-> `.json`)

* **Обновлены функции чтения и записи графа:**

  * Теперь используются только актуальные функции `save_syn1`, `load_syn1`
  * Больше никаких лишних/скрытых полей и нестабильной сериализации

---

### 🛠 **Технические детали**

* Все генераторы построены на принципе: “однотипные действия — один вызов фабрики”
* Минимум boilerplate-кода, минимум ручных ошибок
* Проект теперь легко поддерживать и развивать: добавить новый тип — просто добавить функцию в фабрику!
* Код стильно отформатирован, компилируется без ошибок

---

### ⚡️ **Для разработчиков**

* **Писать новые генераторы стало проще:** импортируй `node_factories`, собирай граф, сохраняй через `save_syn1`
* **Добавлять новые узлы — просто добавь функцию в `node_factories`**
* **Сериализация/десериализация теперь стандартизированы**

---

### 🗑 **Удалено/устарело**

* Удалены все вызовы и поля:

  * `save_synapse_file`, `load_synapse_file`
  * `entry`, `payload`, `edges` внутри Node
  * Старые конструкторы `Node::literal_int` и подобные (теперь только через фабрику)

---

### 📈 **Что дальше?**

* Расширение type system (например, поддержка сложных типов, проверка на этапе генерации)
* Интеграция pattern matching, FFI, функций
* Новые генераторы, новые тесты, новые CLI-утилиты

---

> **Проект Synapse стал чище, удобнее, модульнее.
> Всё готово для масштабирования и новых экспериментов!**